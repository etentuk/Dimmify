import { StyleSheet } from 'react-native';

const ConnectIpStyles = StyleSheet.create({
  container: {
    flexGrow: 1,
    alignItems: 'center',
    paddingHorizontal: 20,
    justifyContent: 'center',
  },
});

export default ConnectIpStyles;
